---
title: Merchant Hosted Integration
description: Complete guide for merchant-hosted checkout with card encryption and tokenization
---

# Merchant Hosted Integration

The Merchant Hosted Payment Gateway enables merchants to securely manage the payment lifecycle, including order creation, card tokenization, transaction execution, refunds, and real-time webhook notifications.

## Overview

This integration requires:

1. Creating an order with customer details
2. Encrypting card data using AES-256-CBC
3. Initiating the transaction
4. Handling 3DS verification
5. Checking payment status

## Prerequisites

- Client ID, client secret, and card encryption key from the OMPAY merchant dashboard
- Ability to implement AES-256-CBC encryption
- Server capable of making HTTP requests with required headers

## Authentication

All merchant-hosted requests require:

- **Basic Auth**: `Authorization: Basic <base64(clientId:clientSecret)>`
- **X-Signature**: HMAC SHA256 of `apiPath + payload`
- **Merchant Headers**: Browser fingerprint, user agent, domain

See [Authentication](/docs/getting-started/authentication) for details.

## 1. Create Order

### API Endpoint

```
POST {{baseUrl}}/order
```

### Request

```bash
curl -X POST {{baseUrl}}/order \
  --header 'Authorization: Basic <base64-encoded-clientId:clientSecret>' \
  --header 'Content-Type: application/json' \
  --header 'X-Signature: <generated_signature>' \
  --header 'X-MERCHANT-BROWSER-FINGERPRINT: 8357426ac73fcd60b17355ab7de60421' \
  --header 'X-MERCHANT-USER-AGENT: <user-agent>' \
  --header 'X-MERCHANT-DOMAIN: https://www.xyz.com' \
  --header 'X-MERCHANT-IP: 123.123.123.123' \
  --data-raw '{
    "amount": 100,
    "currency": "OMR",
    "description": "Test Purchase",
    "customerFields": {
      "name": "John Doe",
      "email": "johndoe@example.com",
      "phone": "1234567890"
    },
    "uiMode": "checkout"
  }'
```

### Request Parameters

| Parameter | Mandatory | Description | Type | Example |
|-----------|-----------|-------------|------|---------|
| `amount` | Yes | Transaction amount (up to 2 decimals) | Numeric | `100` |
| `currency` | Yes | Currency code | String | `"OMR"` |
| `uiMode` | Yes | Must be `"checkout"` | String | `"checkout"` |
| `customerFields.name` | Yes | Customer name | String | `"John Doe"` |
| `customerFields.email` | Yes | Customer email | String | `"johndoe@example.com"` |
| `customerFields.phone` | Yes | Customer phone | String | `"1234567890"` |
| `receiptId` | No | Unique identifier (up to 40 chars) | String | `"R12345678910"` |
| `description` | No | Transaction description | String | `"Test Purchase"` |

### Success Response

```json
{
  "resCode": 200,
  "status": "success",
  "data": {
    "orderId": "wb-70c20048-9dc8-4fc3-858e-0a3a5c742190",
    "receiptId": "R12345678910",
    "amount": 100,
    "currency": "OMR"
  }
}
```

### Failure Responses

| ResCode | Status | Description |
|---------|--------|-------------|
| `422` | failure | Incorrect UI mode |
| `401` | failure | Missing authorization header |
| `401` | failure | Invalid or inactive client credentials |
| `500` | failure | Internal server error |

## 2. Encrypt Card Data

Card details must be encrypted using AES-256-CBC with the card encryption key from your merchant dashboard.

### Encryption Function (Node.js)

```javascript
const crypto = require('crypto');

function encryptData(data, aesKey) {
  const iv = crypto.randomBytes(16).toString('hex');
  const cipher = crypto.createCipheriv(
    'aes-256-cbc',
    Buffer.from(aesKey, 'hex'),
    Buffer.from(iv, 'hex'),
  );
  cipher.setAutoPadding(true);
  const encryptedData = Buffer.concat([
    cipher.update(JSON.stringify(data), 'utf8'),
    cipher.final(),
  ]).toString('hex');
  return iv + '.' + encryptedData;
}
```

### Card Data Format

```javascript
const data = {
  cardNumber: '4111111111111111',
  cardExpMonth: '02',
  cardExpYear: '27',
  cardCVV: '123',
};
const encryptedCardDetails = encryptData(data, aesKey);
```

### Tokenized Card Format

```javascript
const data = {
  cardCVV: '123',
  digitalCardId: '685b168ebf56f28d31f64428',
};
const encryptedCardDetails = encryptData(data, aesKey);
```

## 3. Initiate Transaction

### API Endpoint

```
POST {{baseUrl}}/transaction/initiate
```

### Request

```bash
curl --location --request POST '{{baseUrl}}/transaction/initiate' \
  --header 'Authorization: Basic <base64-encoded-clientId:clientSecret>' \
  --header 'Content-Type: application/json' \
  --header 'X-Signature: <generated_signature>' \
  --header 'X-MERCHANT-BROWSER-FINGERPRINT: 8357426ac73fcd60b17355ab7de60421' \
  --header 'X-MERCHANT-USER-AGENT: <user-agent>' \
  --header 'X-MERCHANT-DOMAIN: https://www.xyz.com' \
  --header 'X-MERCHANT-IP: 123.123.123.123' \
  --data-raw '{
    "orderId": "wb-158c4274-a7f0-4540-a5c2-44a03b5a0696",
    "encryptedCardDetails": "<encrypted card details>",
    "paymentMethod": "card",
    "cardHolderName": "test",
    "redirectionUrl": "https://api.uat.gateway.ompay.com/pg/v1/transactions/test/callback",
    "paymentMode": "card",
    "secureCard": true,
    "skipCVVOnlyFlag": false,
    "skipCVVandOTP": false,
    "apiType": "hosted"
  }'
```

### Request Parameters

| Parameter | Mandatory | Description | Type | Example |
|-----------|-----------|-------------|------|---------|
| `orderId` | Yes | Order ID from create order | String | `"wb-158c4274-a7f0-4540-a5c2-44a03b5a0696"` |
| `encryptedCardDetails` | Yes | AES-256-CBC encrypted card data | String | `<encrypted>` |
| `paymentMethod` | Yes | Must be `"card"` | String | `"card"` |
| `cardHolderName` | Yes | Name on card | String | `"test"` |
| `redirectionUrl` | Yes | URL for 3DS redirect | String | `"https://.../callback"` |
| `paymentMode` | Yes | `"card"` or `"token"` | Enum | `"card"` |
| `apiType` | Yes | Must be `"hosted"` | String | `"hosted"` |
| `skipCVVandOTP` | Yes | Skip CVV and OTP | Boolean | `false` |
| `skipCVVOnlyFlag` | Yes | Skip CVV only | Boolean | `false` |
| `secureCard` | No | Enable tokenization | Boolean | `true` |

### Transaction Modes

| paymentMethod | apiType | paymentMode | secureCard | skipCVVandOTP | skipCVVOnlyFlag | Behavior |
|---------------|---------|-------------|------------|---------------|-----------------|----------|
| card | hosted | card | true | false | false | Enter card + CVV + OTP |
| card | hosted | card | true | true | false | Use saved card, no auth |
| card | hosted | card | true | false | true | Enter card + OTP only |
| card | hosted | card | true | true | true | **Error** |
| card | hosted | token | false | false | false | Use token + CVV + OTP |
| card | hosted | token | false | true | false | Use token, no auth |
| card | hosted | token | false | false | true | Use token + OTP only |
| card | hosted | token | false | true | true | **Error** |

### Success Response (Without Tokenization)

```json
{
  "resCode": 200,
  "status": "success",
  "data": {
    "paymentId": "pay798378b8c272451f90cb2b491a86ed17",
    "orderId": "wb-70c20048-9dc8-4fc3-858e-0a3a5c742190",
    "receiptId": "R12345678910",
    "paymentStatus": "pending",
    "amount": 100,
    "currency": "OMR",
    "redirectionData": {
      "method": "POST",
      "formData": "<form>...</form>"
    }
  }
}
```

### Success Response (With Tokenization)

```json
{
  "resCode": 200,
  "status": "success",
  "data": {
    "paymentId": "pay798378b8c272451f90cb2b491a86ed17",
    "orderId": "wb-70c20048-9dc8-4fc3-858e-0a3a5c742190",
    "receiptId": "R12345678910",
    "paymentStatus": "pending",
    "amount": 100,
    "currency": "OMR",
    "redirectionData": {
      "formData": "<form>...</form>"
    },
    "securedCardDetails": {
      "customerId": "4bf4f6a2-ba0a-420f-89a9-e718007ab11a",
      "digitalCardId": "685a421bca91e50caa13300c"
    }
  }
}
```

### Failure Responses

| ResCode | Status | Description |
|---------|--------|-------------|
| `404` | failure | No route found |
| `409` | failure | Sales transactions not allowed |
| `422` | failure | FRM validation failed |
| `409` | failure | Transaction already in progress |
| `401` | failure | Missing or invalid authorization |
| `500` | failure | Internal server error |

## 4. 3DS Verification

After initiating the transaction, handle the `redirectionData` in the response:

- **`method`**: Either `POST` or `GET`
- **`formData`**: HTML form for POST method that should auto-submit

### POST Method

The form will auto-submit to the 3DS page:

```html
<form action="https://certpayments.oabipay.com/trxns/VPAS.htm?..." method="POST" id="omannet_re_direct">
</form>
<script type="text/javascript">
  document.getElementById('omannet_re_direct').submit();
</script>
```

After 3DS completion, the customer is redirected to your `redirectionUrl`. Call the payment status API to get the final status.

## 5. Check Payment Status

### API Endpoint

```
GET {{baseUrl}}/transaction/status/{paymentId}
```

### Request

```bash
curl --location --request GET '{{baseUrl}}/transaction/status/{paymentId}' \
  --header 'Authorization: Basic <base64-encoded-clientId:clientSecret>' \
  --header 'X-Signature: <generated_signature>' \
  --header 'X-MERCHANT-BROWSER-FINGERPRINT: 8357426ac73fcd60b17355ab7de60421' \
  --header 'X-MERCHANT-USER-AGENT: <user-agent>' \
  --header 'X-MERCHANT-DOMAIN: https://www.xyz.com' \
  --header 'X-MERCHANT-IP: 123.123.123.123'
```

### Sales Transaction Response

```json
{
  "resCode": 200,
  "status": "success",
  "data": {
    "paymentId": "paycbaff3b9dc5443f0ba0997970ebeddfa",
    "orderId": "wb-bf35c7de-1e8f-425a-8833-bc23d609b256",
    "receiptId": "",
    "paymentStatus": "pending",
    "paymentMethod": "card",
    "transactionType": "sales",
    "amount": "1.000",
    "currency": "OMR",
    "initiatedAt": "2025-05-29T07:21:19.845Z",
    "completedAt": "2025-05-29T07:21:19.846Z",
    "signature": "10ff8c4ddbeb8fcf24fcc110acb31599690a8057ecd3a7141b22354f9b73337f",
    "description": "Transaction pending",
    "paymentDetails": {
      "cardNetwork": "visa",
      "cardType": "credit",
      "cardUsageType": "domestic"
    }
  }
}
```

### Refund Transaction Response

```json
{
  "resCode": 200,
  "status": "success",
  "data": {
    "paymentId": "paycbaff3b9dc5443f0ba0997970ebeddfa",
    "orderId": "wb-bf35c7de-1e8f-425a-8833-bc23d609b256",
    "receiptId": "",
    "paymentStatus": "success",
    "paymentMethod": "card",
    "transactionType": "refund",
    "amount": "1.000",
    "currency": "OMR",
    "initiatedAt": "2025-05-29T07:21:19.845Z",
    "completedAt": "2025-05-29T07:21:19.846Z",
    "signature": "10ff8c4ddbeb8fcf24fcc110acb31599690a8057ecd3a7141b22354f9b73337f",
    "description": "Refund success"
  }
}
```

## 6. Initiate Refund

### API Endpoint

```
POST {{baseUrl}}/transaction/refund
```

### Request

```bash
curl --location --request POST '{{baseUrl}}/transaction/refund' \
  --header 'Authorization: Basic <base64-encoded-clientId:clientSecret>' \
  --header 'X-Signature: <generated_signature>' \
  --header 'X-MERCHANT-BROWSER-FINGERPRINT: 8357426ac73fcd60b17355ab7de60421' \
  --header 'X-MERCHANT-USER-AGENT: <user-agent>' \
  --header 'X-MERCHANT-DOMAIN: https://www.xyz.com' \
  --header 'X-MERCHANT-IP: 123.123.123.123' \
  --data-raw '{
    "paymentId": "051ca748-14e2-4507-a39a-e45a28afc10b",
    "amount": 100.00
  }'
```

### Request Parameters

| Parameter | Mandatory | Description | Type | Example |
|-----------|-----------|-------------|------|---------|
| `paymentId` | Yes | Payment ID to refund | String | `"051ca748-14e2-4507-a39a-e45a28afc10b"` |
| `amount` | Yes | Amount to refund | Numeric | `100.00` |

### Success Response

```json
{
  "resCode": 200,
  "status": "success",
  "data": {
    "refundId": "ref123456789",
    "paymentId": "pay798378b8c272451f90cb2b491a86ed17",
    "orderId": "wb-70c20048-9dc8-4fc3-858e-0a3a5c742190",
    "receiptId": "R12345678910",
    "paymentStatus": "success",
    "paymentMethod": "card",
    "amount": 100.0,
    "currency": "OMR",
    "initiatedAt": "2025-05-29T07:21:19.845Z",
    "compltedAt": "2025-05-29T07:21:19.846Z",
    "signature": "10ff8c4ddbeb8fcf24fcc110acb31599690a8057ecd3a7141b22354f9b73337f"
  }
}
```

## 7. Card Tokenization

### List Digital Cards

```bash
GET {{baseUrl}}/customer/{customerId}/digitalCards
```

### Get Specific Digital Card

```bash
GET {{baseUrl}}/customer/{customerId}/digitalCards/{digitalCardId}
```

### Delete Digital Card

```bash
DELETE {{baseUrl}}/customer/{customerId}/digitalCards/{digitalCardId}
```

### Digital Card Response

```json
{
  "resCode": 200,
  "status": "success",
  "data": {
    "digitalCards": [
      {
        "digitalCardId": "685a421bca91e50caa13300c",
        "network": "visa",
        "cardType": "credit",
        "status": "ACTIVE",
        "panLastFour": "1111",
        "createdAt": "2025-06-24T06:13:47.869Z",
        "updatedAt": "2025-06-24T06:13:47.869Z"
      }
    ],
    "remainingLimit": 4
  }
}
```

## Next Steps

- [Authentication](/docs/getting-started/authentication)
- [Signature Verification](/docs/reference/signature-verification)
- [Webhooks](/docs/reference/webhooks)
- [Test Cards](/docs/reference/test-cards)
- [API Reference](/docs/api-reference)
